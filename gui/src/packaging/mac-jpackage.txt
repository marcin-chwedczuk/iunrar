--name ${app.name}
--app-version ${app.version}
--icon ${project.basedir}/app-icon.icns
--file-associations ${project.build.directory}/packaging/file-association-rar.txt
--module ${main-class}
--type dmg
--runtime-image ${project.build.directory}/gui
--dest ${project.build.directory}
--temp ${project.build.directory}/installer-work
--verbose